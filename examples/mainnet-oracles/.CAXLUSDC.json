{
  "output_id": "axlusdc_usd",
  "templates": [
    {
      "template_id": "response_handler",
      "adapters": [
        {
          "adapter": "unresponsive_handler",
          "config": {
            "strategy": "use_last",
            "grace_duration": "120"
          }
        }
      ]
    }
  ],
  "jobs": [
    {
      "output_id": "osmosis_axlusdc_osmo_pool",
      "adapters": [
        {
          "adapter": "fetcher_multiple",
          "config": {
            "custom_node_url_key": "osmosis_678_1",
            "default_node_host": "https://api-osmosis.carbon.network",
            "default_node_path": "/osmosis/gamm/v1beta1/pools/678",
            "paths": "[\"pool.pool_assets.1.token.amount\", \"pool.pool_assets.0.token.amount\"]",
            "timeout": "10"
          }
        },
        {
          "template": "response_handler"
        }
      ]
    },
    {
      "output_id": "osmosis_axlusdc_osmo",
      "adapter": "math_filter",
      "input_id": "osmosis_axlusdc_osmo_pool",
      "config": {
        "operation": "divide"
      }
    },
    {
      "output_id": "osmo_usd",
      "adapters": [
        {
          "adapter": "oracle_result_fetcher",
          "config": {
            "oracle_id": ".COSMO",
            "stale_allowance": "900"
          }
        }
      ]
    },
    {
      "output_id": "osmosis_axlusdc_usd",
      "adapter": "math_filter",
      "config": {
        "operation": "multiply",
        "input_ids": "[\"osmosis_axlusdc_osmo\", \"osmo_usd\"]"
      }
    },
    {
      "adapter": "static_handler",
      "output_id": "static_usd",
      "config": {
        "value": "1"
      }
    },
    {
      "output_id": "axlusdc_usd",
      "adapter": "math_filter",
      "input_id": "",
      "config": {
        "operation": "min",
        "input_ids": "[\"osmosis_axlusdc_usd\",\"static_usd\"]"
      }
    },
    {
      "output_id": "axlusdc_usd",
      "input_id": "axlusdc_usd",
      "adapter": "float_handler",
      "config": {
        "operation": "round",
        "precision": "6"
      }
    }
  ]
}
